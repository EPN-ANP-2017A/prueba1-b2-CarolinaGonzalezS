{% extends 'base.html.twig' %}

{% block body %}
<center>
    <h1>Registro de plato</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('restaurante_plato_index') }}">Back to the list</a>
        </li>
    </ul>
</center>
{% endblock %}
